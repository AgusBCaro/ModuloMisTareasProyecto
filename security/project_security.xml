<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data np="1">
        <!-- Regla de Registro (Record Rule) para restringir Tareas según la preferencia 'Solo Mis tareas' -->
        <record id="rule_project_task_only_my_tasks" model="ir.rule">
            <field name="name">Regla de Proyectos: Solo Mis Tareas si el usuario lo activa</field>
            <field name="model_id" ref="project.model_project_task"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[
                '|',
                ('create_uid.only_my_tasks', '=', False),
                '|',
                ('user_ids', 'in', [user.id]),
                ('user_id', '=', user.id)
            ]</field>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>
    </data>
</odoo>
